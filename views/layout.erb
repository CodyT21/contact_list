<!doctype html>
<html lang="en-US">
  <head>
    <title>Contact List App</title>
    <meta charset="UTF-8">
  </head>
  <body>
    <% if session[:message] %>
      <p class="message"><%= session.delete(:message) %></p>
    <% end %>
    <main>
      <%== yield %>
    </main>
  </body>
</html>